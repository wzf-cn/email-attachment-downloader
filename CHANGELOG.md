# 更新记录

### 1.5.1 一键生成主题说明
规则中填写本次邮件主题后，可生成可编辑、可复制的提交说明，包含主题格式、占位项替换要求、关键词范围及名单填写要求。不自动公开真实名单或固定秘钥。


### 1.5.0 收件规则检索范围
新增主题、正文、附件名三个独立检索选项，可组合匹配。默认仅主题，兼容旧规则；模板保留所选范围。正文及附件名检索需要先接收邮件，仍受整封邮件大小限制，附件内部内容不参与检索。


### 1.4.5 Star 窗口布局
精简标题与说明，GitHub / Gitee 采用等宽平台按钮，统一间距，将关闭按钮放在右下角。


### 1.4.4 归档检索
归档记录增加即时检索框，按主题、发件邮箱、收件邮箱或规则名称筛选，支持清空。刷新后保留检索词，导出 CSV 使用当前筛选结果。


### 1.4.3 PayPal 品牌按钮
PayPal 赞助入口使用官方品牌 Logo、浅蓝背景与蓝色边框和文字，支持中英文。


### 1.4.2 收款卡片背景
微信与支付宝恢复绿色、蓝色背景，背景框统一尺寸；保留二维码白边及下方平台标识。


### 1.4.1 应用图标
将手握三个信封图标接入程序文件、窗口、托盘、安装器和卸载器；提供多种尺寸以适配 Windows 显示。


### 1.4.0 每条规则独立检查频率
移除固定开头设置；关键词负责检索，结构模式继续校验主题项目和结尾名单或秘钥。每条规则可设置 1–1440 分钟检查频率，旧规则沿用原全局间隔。立即检查和重新导出可检查全部规则。较慢规则仍可归档同一封邮件，回复与错误计数保持去重。


### 1.3.3 启动检查更新
启动时并行查询 GitHub 和 Gitee 正式版本，发现新版后提示打开官方发布页下载。运行设置可关闭启动检查或手动检查；网络异常不影响收信。

### 1.3.2 赞助界面精简
仅展示二维码及下方的平台标识和名称，去掉收款宣传背景，保留 PayPal 入口。二维码原图保持不变。

### 1.3.1 PayPal 赞助
赞助窗口新增 PayPal.Me 入口，提供中英文按钮和悬停说明。只在用户点击后打开浏览器，微信与支付宝收款码继续保留。

### 1.1.0 界面更新

采用 Apache-2.0 许可的 AntdUI。左侧导航用于切换邮箱与规则、归档、回复、停收、异常、日志和运行设置。开始、检查和暂停始终在窗口底部。

规则编辑分为“触发与主题”“下载与存放”“回复与安全”“测试识别”。主题示例随设置更新；切换页面保留未保存的填写内容。邮箱登录和收发服务器分开设置。数据目录和现有规则格式不变。

开发验证使用独立临时目录和模拟邮件；`--smoke-test` 配合 `MAILINTAKE_CAPTURE` 可导出各页面及最小窗口截图，不接入真实邮箱。

### 1.2.0 私密反馈
意见反馈改为 HTTPS 直接提交到自建后台，无需邮箱或 Gitee 账号。支持可选联系方式、本地草稿、失败重试去重。后台部署、备份和迁移见 [部署说明](docs/FEEDBACK_DEPLOYMENT.md)。

### 1.2.1 连续错误停收
默认连续错误 3 次停收：前 2 次统一错误回复，第 3 次停收通知。运行设置可调整为 1–20 次，保存并开始后生效。完整主题校验成功清零；重复检查和重新导出不重复计数或清零。阈值跨本机绑定邮箱共享。升级首次启用时清零未停收发件人的旧累计次数，已有停收状态和历史记录保留。

### 1.2.2 操作提示
软件内按钮和主要选框增加鼠标悬停说明：停留约半秒显示，解释操作作用与影响。

### 1.2.3 支持项目
顶部新增“点个 Star”和“赞助支持”。可选择 GitHub 或 Gitee 项目页；赞助窗口提供微信、支付宝收款码及查看原图入口。打赏自愿，不影响软件功能。

### 1.3.0 中英文与邮箱自动配置
新增界面语言切换及常见邮箱的完整收发参数识别。规则的内部值保持兼容；名单姓名校验同时识别字段名“姓名”和“Name”。自动默认值保留手动服务器设置。新增域名、协议切换和语言往返检查。
