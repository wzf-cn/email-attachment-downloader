# 安全问题

请勿在公开 Issue 中填写密码、邮箱授权码、私钥、真实邮件或用户反馈原文。

发现安全问题时，可通过官方应用的“意见反馈”私下报告最小复现步骤和影响，留下可联系的方式；不要上传利用所得的数据。也可使用 GitHub 的私密漏洞报告功能（仅在仓库显示该入口时）。维护者尚未承诺固定响应时间。

目前维护最新发布版本。历史版本可能包含已修复问题。安装包暂未进行代码签名；应从本项目 Releases 获取，并核对随发布提供的 SHA-256 文件。哈希用于检查下载完整性，不能代替数字签名。

分发自己的修改版时，请配置自己的反馈端点，并明确告知用户数据接收者。不要在客户端嵌入管理密钥。后台管理端口应只监听本机，通过 SSH 访问。
